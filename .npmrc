registry=https://registry.npmmirror.com
ignore-scripts=false

# 走代理：与系统/Clash HTTP 端口一致时取消注释并改地址；也可在 shell 里设置 HTTP_PROXY / HTTPS_PROXY（npm 会用于下载）
# proxy=http://127.0.0.1:7890
# https-proxy=http://127.0.0.1:7890
